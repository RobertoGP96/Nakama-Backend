model Resource {
    id      Int    @default(autoincrement()) @unique
    address String @unique

    type categoryName

    createat DateTime @default(now())
    updateat DateTime @updatedAt

    e_founds_count  Int   @default(0)
    e_pending_count Int   @default(0)
    e_founds        Int[] @default([])
}
